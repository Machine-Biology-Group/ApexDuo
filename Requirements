# ApexDuo — Python dependencies
# Reference environment used in the study: PyTorch 1.11.0 (CUDA 11.3 build), Python 3.9.
# Pins below are the tested versions; see README for GPU vs CPU install notes.

torch==1.11.0            # study used the +cu113 (CUDA 11.3) wheel; a CPU wheel also works for inference
numpy>=1.21,<1.27
pandas>=1.3
scipy>=1.7
scikit-learn>=1.0        # KernelDensity (KDE sampling) and cross-validation
matplotlib>=3.4
biopython>=1.79          # FASTA parsing of peptide datasets

# Cell-penetrating-peptide physicochemical descriptors (feature engineering in the CPP predictor)
modlamp>=4.3

# Optional — imported by some scripts but not required for core inference/generation
rdkit-pypi>=2022.3.1